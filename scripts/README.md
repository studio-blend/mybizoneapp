# Scripts

Repo-level utilities. Each runs via `pnpm tsx scripts/<file>.ts`.

## generate-license.ts

Offline RSA-signed license key generator for self-hosted deployments.
See M5 implementation for usage.
