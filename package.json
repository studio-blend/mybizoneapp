{
  "name": "mybizone",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @mybizone/web dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter @mybizone/web start",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter @mybizone/web test:e2e",
    "db:generate": "pnpm --filter @mybizone/db generate",
    "db:migrate": "pnpm --filter @mybizone/db migrate",
    "db:push": "pnpm --filter @mybizone/db push",
    "db:studio": "pnpm --filter @mybizone/db studio",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "typescript": "5.6.3"
  }
}
